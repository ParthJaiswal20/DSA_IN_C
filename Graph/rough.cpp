#include <stdio.h>
#include <stdlib.h>
int main()
{
    int n, m;
    // n for nodes and m for graphs
    printf("enter the number of nodes and edges");
    scanf("%d %d", &n, &m);
    int a;
    int b;
    int arr[n + 1][n + 1];
    
    }
}